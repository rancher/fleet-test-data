defaultNamespace: donotdeploy
targetCustomizations:
  - name: test
    doNotDeploy: false   # 'false': Resources will be installed on all cluster(s).
    clusterSelector:
      matchLabels:
        test: imported
