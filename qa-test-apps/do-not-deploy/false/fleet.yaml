defaultNamespace: donotdeploy
targetCustomizations:
  - name: test
    doNotDeploy: false   # 'false': Resources will installed on all cluster(s).
    clusterSelector:
      matchLabels:
        test: imported
