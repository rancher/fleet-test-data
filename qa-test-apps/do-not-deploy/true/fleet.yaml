defaultNamespace: donotdeploy
targetCustomizations:
  - name: test
    doNotDeploy: true   # 'true': Resources will be installed on cluster(s) having below labels.
    clusterSelector:
      matchLabels:
        key_resources: deploy_true
