# This file and all contents in it are OPTIONAL.

# The namespace this chart will be installed and restricted to,
# if not specified the chart will be installed to "default"
namespace: fleet-helm-oci-example

# Custom helm options
helm:
  # The release name to use. If empty a generated release name will be used
  releaseName: fleet-test-configmap

  # For OCI registries, the full oci:// URL (including chart name) goes in
  # repo. For HTTPS helm repos, repo is the base URL and chart is the chart
  # name. For go-getter URLs or direct chart URLs, chart is used instead.
  repo: "oci://ghcr.io/rancher/fleet-test-configmap-chart"

  # Used if repo is set to look up the version of the chart
  version: "0.1.0"

  # Force recreate resource that can not be updated
  force: false

  # How long for helm to wait for the release to be active. If the value
  # is less that or equal to zero, we will not wait in Helm
  timeoutSeconds: 0

  # Custom values that will be passed as values.yaml to the installation
  values:
    replicas: 2
